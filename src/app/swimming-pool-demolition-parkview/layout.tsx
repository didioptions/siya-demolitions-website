
import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Swimming Pool Demolition Parkview | Pool Removal Experts',
  description: 'Professional swimming pool demolition in Parkview. Safe removal of old pools, with special care for heritage properties and established gardens. Call for a quote.',
};

export default function SwimmingPoolDemolitionParkviewLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
