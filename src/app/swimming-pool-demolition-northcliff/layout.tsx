import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Swimming Pool Demolition Northcliff | Professional Pool Removal',
  description: 'Licensed swimming pool demolition contractors in Northcliff, Johannesburg. Safe pool removal on steep slopes, including rubble clearing and site prep.',
};

export default function SwimmingPoolDemolitionNorthcliffLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
