import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Swimming Pool Demolition Roodepoort | Fast Pool Removal Experts',
  description: 'Affordable swimming pool demolition in Roodepoort. Fast, safe pool removal with full rubble cleanup for the West Rand. Call now for a free quote.',
};

export default function SwimmingPoolDemolitionRoodepoortLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
