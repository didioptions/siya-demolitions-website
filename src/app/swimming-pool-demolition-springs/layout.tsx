import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Swimming Pool Demolition Springs | Fast Pool Removal Experts',
  description: 'Affordable swimming pool demolition in Springs. Fast, safe pool removal with full rubble cleanup for residential and commercial properties. Call now for a free quote.',
};

export default function SwimmingPoolDemolitionSpringsLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
