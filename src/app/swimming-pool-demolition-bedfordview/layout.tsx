import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Swimming Pool Demolition Bedfordview | Professional Pool Removal',
  description: 'Licensed swimming pool demolition contractors in Bedfordview, Johannesburg. Safe removal of all pool types for high-end properties, including rubble clearing and site prep.',
};

export default function SwimmingPoolDemolitionBedfordviewLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
