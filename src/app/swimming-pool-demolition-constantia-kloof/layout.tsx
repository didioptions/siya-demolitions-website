
import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Swimming Pool Demolition Constantia Kloof | Pool Removal',
  description: 'Expert swimming pool demolition in Constantia Kloof. Safe, insured removal of pools on sloped properties, with full rubble clearing. Call for a site assessment.',
};

export default function SwimmingPoolDemolitionConstantiaKloofLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
