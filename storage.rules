rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Allow public read access to gallery images
    match /gallery/{allPaths=**} {
      allow read: if true;
      // Only allow writes if the user is authenticated (even anonymous is fine).
      allow write: if request.auth != null;
    }
  }
}
